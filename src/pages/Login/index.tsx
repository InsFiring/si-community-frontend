import Modal from '@/src/components/common/Modal';
import * as S from './style';

const Login = () => {
    return (
        <S.LoginContainer>
            Login
            <Modal>Modal</Modal>
        </S.LoginContainer>
    );
};

export default Login;
